/* ========================================================
   CONFIG.JS — Configuração base da aplicação
   --------------------------------------------------------
   01A.1  GCAL_WORKER_URL   URL do Worker Google Calendar
   01A.2  UI                Escala de tamanhos de fonte
   ======================================================== */

/* ---- 01A.1 — URL do Worker Google Calendar ---- */
export const GCAL_WORKER_URL = "https://gc-gcal.dr-joao-morais.workers.dev";

/* ---- 01A.2 — Escala de tamanhos de fonte (agenda + shell) ---- */
export const UI = {
  fs12: 13,
  fs13: 14,
  fs14: 15,
  fs16: 17,
  fs18: 19, // nome do doente
};
